import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stackhorizon.orario',
  appName: 'Orario universitario',
  webDir: 'public'
};

export default config;
